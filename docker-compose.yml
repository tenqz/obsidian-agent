services:
  obsidian-agent:
    build:
      context: .
    ports:
      - "${AGENT_PORT:-18080}:8000"
    env_file:
      - .env
    environment:
      APP_ENV: "local"
      VAULT_PATH: "/vault"
    volumes:
      - "${OBSIDIAN_VAULT_PATH:-./vault}:/vault"
